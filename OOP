1.内存分段：
1.1 栈空间段
    特点：空间小、CPU访问速度快。
    使用户存放程序中临时创建的变量。是临时数据寄存、交换的内存区。
    
1.2 堆空间段
    用于存放进程运行中被动态分配的内存段，它大小并不固定，可动态扩张或缩减。用于存储数据长度可变或占用内存比较大的数据（字符串、数组和对象存在于这段内存中）
    
1.3 数据段
    用来存放可执行文件中已初始化全局变量，换句话说就是存放程序静态分配的变量。
